export const Navbar = () => {
    return (
        <nav className="p-2 flex justify-between items-center">
            <h1>CMU Courses</h1>
            <button type="button">Login</button>
        </nav>
    );
};
